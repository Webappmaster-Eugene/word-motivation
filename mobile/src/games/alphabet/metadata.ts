import type { GameMetadata } from '@/games/types';

export const alphabetMetadata: GameMetadata = {
  id: 'alphabet',
  title: 'Алфавит',
  subtitle: 'Буквы, слова и зверята',
  minAge: 6,
  maxAge: 12,
  tags: ['letters', 'speech', '3d'],
  version: '0.1.0',
};
